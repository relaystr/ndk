/// Tuple class to hold two values
class Tuple<T, U> {
  /// The first value.
  final T first;

  /// The second value.
  final U second;

  /// Creates a new tuple.
  Tuple(this.first, this.second);
}
