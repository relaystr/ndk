name: ndk_amber
description: Nostr Development Kit - the most performant lib for all your nostr usecases
version: 0.1.0-dev994
homepage: https://github.com/relaystr/dart_ndk

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=1.17.0"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.7
  amberflutter: ^0.0.9
  logger: ^2.4.0
  ndk:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  mockito: ^5.0.17
  integration_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: relaystr.ndk
        pluginClass: DartNdkPlugin
